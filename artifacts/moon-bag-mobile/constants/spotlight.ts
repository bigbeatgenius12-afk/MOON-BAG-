// Spotlight mode: when set, the app surfaces ONLY this launched coin.
// Set to null to bring every coin back. Keep in sync with the website's
// artifacts/moon-bag/src/lib/spotlight.ts.
export const SPOTLIGHT_TICKER: string | null = null;
